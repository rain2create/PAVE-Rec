# Experiment Runs

Each run will write to its own directory and retain the resolved configuration,
seed, dataset and model versions, metrics, cost summary, and agent JSONL trace.

Run outputs are ignored by Git.
