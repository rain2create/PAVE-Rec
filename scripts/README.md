# Scripts

This directory is reserved for thin shell or environment wrappers.

Reusable logic belongs in `src/pave_rec`. Python execution entry points belong
in `pave_rec.cli`, so the same behavior can be called from the command line,
tests, notebooks, or another Python application.
